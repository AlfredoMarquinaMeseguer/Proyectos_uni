#include <string>
struct Solucion{
    int * equipoA;
    int * equipoB;
};

int diferencia(struct Solucion equipos){
    // Codigo en phyton
    // a = reduce(lambda x, y: x + y, equipos.equipoA)
    // b = reduce(lambda x, y: x + y, equipos.equipoB)
    // return abs(a - b)


    return 0;
}

struct Solucion backtracking(int * candidatos, int indiceCandidatos, struct Solucion equipos, int dimensiones){
    if (indiceCandidatos == dimensiones){
        
    }
}